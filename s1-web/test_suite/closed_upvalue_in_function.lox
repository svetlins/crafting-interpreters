fun toplevel() {
  var pad1 = 1;
  var pad2 = 2;

  fun makeCounter() {
    var count = 0;

    fun counter() {
      count = count + 1;
      return count - 1;
    }

    return counter;
  }

  var counter1 = makeCounter();
  var counter2 = makeCounter();

  print counter1(); // => 0.0
  print counter1(); // => 1.0
  print counter1(); // => 2.0
  print counter2(); // => 0.0
  print counter2(); // => 1.0
  print counter1(); // => 3.0
  print counter1(); // => 4.0
  print counter2(); // => 2.0
}

toplevel();
